# == Schema Information
#
# Table name: roles
#
#  id         :bigint           not null, primary key
#  name       :string
#  created_at :datetime         not null
#  updated_at :datetime         not null
#

admin:
  name: Admin

manager:
  name: Manager

accounting:
  name: Accounting

owner:
  name: Owner

tenant:
  name: Tenant
