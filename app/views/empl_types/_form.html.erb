<%= form_with(model: empl_type, local: true) do |form| %>
  <% if empl_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(empl_type.errors.count, "error") %> prohibited this empl_type from being saved:</h2>

      <ul>
        <% empl_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
